<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vip001.framemonitor">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ExApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".exam.Example1Activity" />
        <activity android:name=".exam.Example2Activity" />
        <activity android:name=".exam.Example5Activity" />
        <activity android:name=".exam.Example3Activity"></activity>
        <activity android:name="com.vip001.framemonitor.exam.Example4Activity"></activity>
    </application>

</manifest>